import type {PublicKey} from '~/common/crypto';

/**
 * Props accepted by the `ThreemaIdInfoInfoModal` component.
 */
export interface ThreemaIdInfoInfoModalProps {
    readonly publicKey?: PublicKey;
}
