import type {AppServicesForSvelte} from '~/app/types';

/**
 * Props accepted by the `ConversationView` component.
 */
export interface ConversationViewProps {
    readonly services: AppServicesForSvelte;
}
