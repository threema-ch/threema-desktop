/**
 * Props accepted by the `Tags` component.
 */
export interface TagsProps {
    readonly isArchived?: boolean;
    readonly isCreator?: boolean;
    readonly isInactive?: boolean;
    readonly isInvalid?: boolean;
}
