import type {AppServicesForSvelte} from '~/app/types';

/**
 * Props accepted by the `ContactDetail` component.
 */
export interface ContactDetailProps {
    readonly services: AppServicesForSvelte;
}
