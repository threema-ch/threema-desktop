variables:
  # Keep in sync with `libthreema`.
  BINARYEN_VERSION: '123'
  FF_DISABLE_UMASK_FOR_DOCKER_EXECUTOR: 'true'
  # Clone the repository from scratch for every job.
  GIT_STRATEGY: clone
  # Clone git repository recursively.
  GIT_SUBMODULE_STRATEGY: recursive
  NODE_VERSION: '22.17.0'
  NVM_VERSION: '0.40.3'
  # Major.minor channel form of RUST_VERSION (e.g. 1.94). Used for rustup and Dockerfiles.
  RUST_CHANNEL: '1.94'
  # Keep in sync with `apps/desktop/src/rust/{common, helper, launcher}/rust-toolchain.toml` and
  # `libthreema`. Must be the full semver (e.g. 1.94.0). Used for distribution archive downloads.
  RUST_VERSION: '1.94.0'
  WASM_BINDGEN_VERSION: '0.2.114'
  # Repositories used by the protocol drift checks. They are cloned via `CI_JOB_TOKEN`, which
  # requires this project to be on each repository's job token allowlist.
  STRUCTBUF_TYPESCRIPT_REPO: '${CI_SERVER_HOST}/clients/protocols/structbuf-typescript.git'
  THREEMA_PROTOCOLS_REPO: '${CI_SERVER_HOST}/clients/protocols/threema-protocols.git'

default:
  image: node:${NODE_VERSION}-bookworm
  hooks:
    pre_get_sources_script:
      # FF_DISABLE_UMASK_FOR_DOCKER_EXECUTOR disables the umask here,
      # but executes a chown to the job's container user - if it is not root.
      # In case the last runner's job was not root, it leaves files that are owned by
      # some other user - breaking npm's chown. See:
      # - https://gitlab.com/gitlab-org/gitlab-runner/-/issues/1736
      # - https://gitlab.com/gitlab-org/gitlab/-/issues/368133
      - |
        if [ "$(uname)" = "Linux" ] && [ -d "${CI_PROJECT_DIR}" ]; then
          echo "Reset permissions of path ${CI_PROJECT_DIR} to root:root"
          chown -R root:root "${CI_PROJECT_DIR}"
        fi

# Workflow triggers:
# - Run for tags, branches and merge requests.
# - However, do not trigger a branch workflow when a MR is open. In that case, only run the MR
#   workflow.
workflow:
  rules:
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS && $CI_PIPELINE_SOURCE == "push"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH

# CI stages.
stages:
  - build
  - test
  - package

.container-build-steps: &container-build-steps
  stage: build
  image: $CI_REGISTRY_IMAGE/buildah:latest
  before_script:
    - set -euo pipefail
    - buildah info
    - buildah login -u $DOCKERHUB_USER -p $DOCKERHUB_TOKEN docker.io
    - buildah login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  tags:
    - docker-privileged
  # Build image and push it to the registry
  script:
    # Determine image path
    - export IMAGE_FULL=$CI_REGISTRY_IMAGE/$IMAGE
    - export IMAGE_CACHE=$CI_REGISTRY_IMAGE/cache/$IMAGE
    - 'echo "Image path: ${IMAGE_FULL}"'
    # Pull image from cache
    - buildah pull ${IMAGE_FULL}:latest || true
    # Build image
    - cd containers/$IMAGE
    - buildah build --layers --cache-to ${IMAGE_CACHE} --cache-from ${IMAGE_CACHE} ${BUILD_ARGS:-} -t ${IMAGE_FULL}:latest -f Dockerfile .
    # Push image
    - buildah push ${IMAGE_FULL}:latest

# Common snippets

# Common environment variables for Linux-based jobs that require a NodeJS environment.
.linux_nodejs_variables: &linux_nodejs_variables
  COREPACK_ENABLE_DOWNLOAD_PROMPT: '0'
  COREPACK_HOME: '$CI_PROJECT_DIR/.corepack'
  NVM_DIR: '$CI_PROJECT_DIR/.nvm'
  PNPM_CONFIG_STORE_DIR: '$CI_PROJECT_DIR/.pnpm-store'

# Common config options for Linux-based jobs that require a NodeJS environment.
.linux_nodejs_config: &linux_nodejs_config
  tags:
    - of-dev7
  variables:
    <<: *linux_nodejs_variables
  # Caching config. Note: Keep this in sync with `*linux_flatpak_config`.
  cache:
    key:
      prefix: linux
      files:
        - package.json
        - pnpm-lock.yaml
    paths:
      - .corepack/
      - .nvm/
      - .pnpm-store/

# Common preparation script for Linux-based jobs that require a NodeJS environment.
.linux_nodejs_before_script: &linux_nodejs_before_script # Install `nvm`.
  - mkdir -p "$NVM_DIR" "$COREPACK_HOME"
  - curl -fsSL "https://raw.githubusercontent.com/nvm-sh/nvm/v${NVM_VERSION}/install.sh" | bash
  - . "$NVM_DIR/nvm.sh"
  - nvm --version

  # Install NodeJS and use version from `.nvmrc`.
  - nvm install && nvm use
  - node --version

  # Enable `corepack` shims, so `pnpm` uses the `packageManager` version defined in `package.json`.
  - corepack enable
  - pnpm --version

# Common preparation script for Linux-based jobs that require a Rust (Cargo) environment.
.linux_rust_before_script: &linux_rust_before_script
  - mkdir /rust
  - pushd /rust
  - export RUST_ARCHIVE=rust-${RUST_VERSION}-x86_64-unknown-linux-gnu.tar.gz
  - export RUST_DOWNLOAD_URL=https://static.rust-lang.org/dist/$RUST_ARCHIVE
  - curl --proto '=https' --tlsv1.2 -fsOSL $RUST_DOWNLOAD_URL
  - curl --proto '=https' --tlsv1.2 -s $RUST_DOWNLOAD_URL.sha256 | sha256sum -c -
  - tar -C /rust -xzf $RUST_ARCHIVE --strip-components=1
  - rm $RUST_ARCHIVE
  - ./install.sh
  - popd
  # Show version.
  - cargo --version

# Common config options for Windows-based jobs that require a NodeJS environment.
.windows_nodejs_config: &windows_nodejs_config
  tags:
    # Request Windows runner (PowerShell).
    - windows
  cache:
    key:
      prefix: windows
      files:
        - package.json
        - pnpm-lock.yaml
    paths:
      - .corepack/
      - .pnpm-store/

# Common preparation script for Windows-based jobs that require a NodeJS environment.
.windows_nodejs_before_script: &windows_nodejs_before_script
  - echo "Preparing NodeJS environment"
  - echo "Original `$Path is $env:Path"
  - echo "Original `$LIB is $env:LIB"

  # Set corepack & pnpm paths.
  - $env:COREPACK_HOME = "$env:CI_PROJECT_DIR\.corepack"
  - $env:COREPACK_ENABLE_DOWNLOAD_PROMPT = "0"
  - $env:PNPM_CONFIG_STORE_DIR = "$env:CI_PROJECT_DIR\.pnpm-store"
  # Print some env variables for debugging purposes.
  - >-
    echo "CI_PROJECT_DIR: $env:CI_PROJECT_DIR"
  - >-
    echo "COREPACK_HOME: $env:COREPACK_HOME"
  - >-
    echo "PNPM_CONFIG_STORE_DIR: $env:PNPM_CONFIG_STORE_DIR"

  # Set up `nvm`.
  - $env:Path += ";C:\\ProgramData\\nvm"
  - $env:NVM_HOME = "C:\\ProgramData\\nvm"
  - $env:NVM_SYMLINK = "C:\\Program Files\\nodejs"

  # Ensure the proper node version is available.
  - echo "Installing and using node version $(Get-Content .nvmrc)"
  - nvm version
  - nvm install (Get-Content .nvmrc)
  - nvm use (Get-Content .nvmrc)

  # Enable corepack for pnpm.
  - corepack enable

  # Set up visual studio environment.
  - "& 'C:\\Program Files (x86)\\Microsoft Visual Studio\\2019\\BuildTools\\Common7\\Tools\\Launch-VsDevShell.ps1'"

  # Print current `$Path` again.
  - echo "Updated `$Path is $env:Path"
  - echo "Updated `$LIB is $env:LIB"

  # Show versions.
  - node --version
  - pnpm --version
  - curl.exe --version

# Common preparation script for Windows-based jobs that require a Rust (Cargo) environment.
.windows_rust_before_script: &windows_rust_before_script
  - echo "Preparing Rust environment"
  - echo "Original `$Path is $env:Path"
  - echo "Original `$LIB is $env:LIB"
  # Update env variables.
  - $env:Path += ";C:\\Users\\Developer\\.cargo\\bin"
  # Print path again.
  - echo "Updated `$Path is $env:Path"
  - echo "Updated `$LIB is $env:LIB"
  # Prevent rustup to self update or auto install new toolchains.
  - rustup set auto-self-update disable
  - rustup set auto-install disable
  # Ensure the toolchain pinned by `RUST_CHANNEL` is installed. No-op if it already is.
  - rustup toolchain install $env:RUST_CHANNEL --profile minimal
  # Show version.
  - cargo "+$env:RUST_CHANNEL" --version

# Build images

image:buildah:
  <<: *container-build-steps
  image: 'quay.io/containers/buildah:latest'
  variables:
    IMAGE: 'buildah'

image:node-browsers:
  <<: *container-build-steps
  needs: [image:buildah]
  variables:
    IMAGE: 'node-browsers'
    BUILD_ARGS: '--build-arg NODE_VERSION=$NODE_VERSION'

image:mdbook:
  <<: *container-build-steps
  needs: [image:buildah]
  variables:
    IMAGE: 'mdbook'
    BUILD_ARGS: >-
      --build-arg DEBIAN_VERSION=13-slim
      --build-arg MDBOOK_PLANTUML_VERSION=2.0.0
      --build-arg MDBOOK_VERSION=0.5.2
      --build-arg PLANTUML_VERSION=1.2026.2
      --build-arg RUST_VERSION=${RUST_CHANNEL}

image:libthreema:
  <<: *container-build-steps
  needs: [image:buildah]
  variables:
    IMAGE: 'libthreema'
    # The double equal sign is intended, buildah will parse that correctly.
    BUILD_ARGS: >-
      --build-arg DEBIAN_VERSION=13
      --build-arg BINARYEN_VERSION=124
      --build-arg RUST_VERSION=${RUST_CHANNEL}
      --build-arg WASM_BINDGEN_VERSION==${WASM_BINDGEN_VERSION}

# Build stage

libthreema:
  stage: build
  needs: [image:libthreema]
  image: $CI_REGISTRY_IMAGE/libthreema:latest
  <<: *linux_nodejs_config
  artifacts:
    paths:
      # Make `turbo` cache available to dependent jobs.
      - .turbo/cache
      # Export `turbo` task summaries.
      - .turbo/runs
      # Make build output available to dependent jobs.
      - packages/libthreema-wasm/libs/libthreema/build
  before_script:
    - set -euo pipefail
    - rustc --version && cargo --version && protoc --version
    - *linux_nodejs_before_script
  script:
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    - pnpm run build:packages:libthreema-wasm --summarize

dist:
  stage: build
  needs: [libthreema]
  <<: *linux_nodejs_config
  artifacts:
    paths:
      # Export `turbo` task summaries.
      - .turbo/runs
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Note: Launcher binary is tested using a separate job, so we skip it here.
    - SKIP_LAUNCHER_BINARY=true pnpm run dist:desktop:consumer-sandbox --summarize

launcher:
  stage: build
  image: rust:${RUST_VERSION}
  before_script:
    - set -euo pipefail
    - rustup component add clippy
  script:
    - set -euo pipefail
    - cargo clippy --manifest-path apps/desktop/src/rust/launcher/Cargo.toml
  variables:
    RUSTFLAGS: '-D warnings'
    THREEMA_BUILD_FLAVOR: consumer-sandbox

pages:
  stage: build
  needs: [image:mdbook]
  image: ${CI_REGISTRY_IMAGE}/mdbook:latest
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
  artifacts:
    paths:
      - public
    expire_in: 3 days
  before_script:
    - mdbook --version
  script:
    - set -euo pipefail
    - cd docs
    - echo 'plantuml-cmd="java -jar /usr/local/lib/plantuml.jar"' >> book.toml
    - mdbook build
    - mv book ../public

# Test stage

audit:
  stage: test
  needs: []
  allow_failure: true
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    - pnpm audit --audit-level moderate

lint:
  stage: test
  needs: [libthreema]
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    - pnpm run lint

test:vitest:
  stage: test
  needs: [libthreema, image:node-browsers]
  image: $CI_REGISTRY_IMAGE/node-browsers:latest
  <<: *linux_nodejs_config
  variables:
    <<: *linux_nodejs_variables
    # Point Vitest's Playwright provider (`executablePath`) at the image's system Chromium.
    CHROMIUM_BIN: /usr/bin/chromium
  artifacts:
    paths:
      # Make `turbo` cache available to dependent jobs.
      - .turbo/cache
      # Export `turbo` task summaries.
      - .turbo/runs
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    - pnpm run test:vitest --summarize

test:desktop:karma:consumer-sandbox:linux:
  stage: test
  needs: [libthreema, image:node-browsers]
  image: $CI_REGISTRY_IMAGE/node-browsers:latest
  <<: *linux_nodejs_config
  artifacts:
    when: always
    reports:
      junit:
        - apps/desktop/junit/consumer-sandbox/karma.xml
    paths:
      # Make `turbo` cache available to dependent jobs.
      - .turbo/cache
      # Export `turbo` task summaries.
      - .turbo/runs
      # Make coverage output available to dependent jobs.
      - apps/desktop/.nyc_output_karma/consumer-sandbox
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Run tests. Note: Outputs coverage reports.
    - pnpm run test:desktop:karma:consumer-sandbox

test:desktop:mocha:consumer-sandbox:linux:
  stage: test
  needs: [libthreema]
  <<: *linux_nodejs_config
  artifacts:
    when: always
    reports:
      junit:
        - apps/desktop/junit/consumer-sandbox/mocha.xml
    paths:
      # Make `turbo` cache available to dependent jobs.
      - .turbo/cache
      # Export `turbo` task summaries.
      - .turbo/runs
      # Make coverage output available to dependent jobs.
      - apps/desktop/.nyc_output_mocha/consumer-sandbox
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Run tests. Note: Outputs coverage reports.
    - pnpm run test:desktop:mocha:consumer-sandbox

test:desktop:mocha:work-onprem:linux:
  stage: test
  needs: [libthreema]
  <<: *linux_nodejs_config
  artifacts:
    when: always
    reports:
      junit:
        - apps/desktop/junit/work-onprem/mocha.xml
    paths:
      # Make `turbo` cache available to dependent jobs.
      - .turbo/cache
      # Export `turbo` task summaries.
      - .turbo/runs
      # Make coverage output available to dependent jobs.
      - apps/desktop/.nyc_output_mocha/work-onprem
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Run tests. Note: Outputs coverage reports.
    - pnpm run test:desktop:mocha:work-onprem

test:desktop:mocha:consumer-sandbox:windows:
  stage: test
  needs: [libthreema]
  <<: *windows_nodejs_config
  variables:
    TURBO_BUILD_ENVIRONMENT: sandbox
    TURBO_BUILD_VARIANT: consumer
  artifacts:
    when: always
    reports:
      junit:
        - apps/desktop/junit/consumer-sandbox/mocha.xml
  before_script:
    - *windows_nodejs_before_script
  script:
    - pnpm ci
    # Run `turbo` task directly, because Windows doesn't support the env variable notation used in
    # `test:desktop:mocha:consumer-sandbox`.
    - pnpm exec turbo run test:mocha --filter=threema-desktop

test:desktop:mocha:work-onprem:windows:
  stage: test
  needs: [libthreema]
  <<: *windows_nodejs_config
  variables:
    TURBO_BUILD_ENVIRONMENT: onprem
    TURBO_BUILD_VARIANT: work
  artifacts:
    when: always
    reports:
      junit:
        - apps/desktop/junit/work-onprem/mocha.xml
  before_script:
    - *windows_nodejs_before_script
  script:
    - pnpm ci
    # Run `turbo` task directly, because Windows doesn't support the env variable notation used in
    # `test:desktop:mocha:work-onprem`.
    - pnpm exec turbo run test:mocha --filter=threema-desktop

test:desktop:playwright:consumer-sandbox:linux:
  stage: test
  needs: [libthreema]
  <<: *linux_nodejs_config
  artifacts:
    expire_in: 1d
    paths:
      - apps/desktop/build/playwright/test-results
  before_script:
    - set -euo pipefail
    - apt-get update && apt-get install -y xvfb openssl libxshmfence1 libnss3 libatk1.0-0 libatk-bridge2.0-0 libdrm2 libgtk-3-0 libgbm1 libasound2
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    - SKIP_LAUNCHER_BINARY=true xvfb-run pnpm run test:desktop:playwright:consumer-sandbox

test:desktop:playwright:work-onprem:linux:
  stage: test
  needs: [libthreema]
  <<: *linux_nodejs_config
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
  artifacts:
    expire_in: 1d
    paths:
      - apps/desktop/build/playwright/test-results
  before_script:
    - set -euo pipefail
    - apt-get update && apt-get install -y xvfb openssl libxshmfence1 libnss3 libatk1.0-0 libatk-bridge2.0-0 libdrm2 libgtk-3-0 libgbm1 libasound2
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    - SKIP_LAUNCHER_BINARY=true xvfb-run pnpm run test:desktop:playwright:work-onprem

test:desktop:playwright:work-sandbox:linux:
  stage: test
  needs: [libthreema]
  <<: *linux_nodejs_config
  artifacts:
    expire_in: 1d
    paths:
      - apps/desktop/build/playwright/test-results
  before_script:
    - set -euo pipefail
    - apt-get update && apt-get install -y xvfb openssl libxshmfence1 libnss3 libatk1.0-0 libatk-bridge2.0-0 libdrm2 libgtk-3-0 libgbm1 libasound2
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    - SKIP_LAUNCHER_BINARY=true xvfb-run pnpm run test:desktop:playwright:work-sandbox

coverage:desktop:consumer-sandbox:
  stage: test
  needs:
    - libthreema
    - test:desktop:karma:consumer-sandbox:linux
    - test:desktop:mocha:consumer-sandbox:linux
  <<: *linux_nodejs_config
  artifacts:
    when: always
    reports:
      # GitLab code coverage integration. See:
      # https://docs.gitlab.com/ci/yaml/artifacts_reports/#artifactsreportscoverage_report.
      coverage_report:
        coverage_format: cobertura
        path: apps/desktop/coverage/cobertura-coverage.xml
    paths:
      - apps/desktop/coverage
  coverage: /All files[^\|]*\|[^\|]*\s+([\d\.]+)/
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Collect coverage. Note: This should use the cached coverage outputs from
    # `test:desktop:karma:consumer-sandbox:linux` and `test:desktop:mocha:consumer-sandbox:linux` so
    # that tests don't need to run again.
    - pnpm run coverage:desktop:consumer-sandbox

# Drift- & misc. sanity-checks.

# Install the pinned `protoc` version required by `@threema/protocol`.
.linux_protoc_before_script: &linux_protoc_before_script
  - |
    set -euo pipefail
    PROTOC_VERSION="$(node -p "require('./packages/protocol/package.json').pins.protoc")"
    echo "Installing protoc ${PROTOC_VERSION}..."
    # `protoc` uses `aarch_64` (not `aarch64`) in its release asset names.
    case "$(uname -m)" in
      x86_64) PROTOC_ARCH="x86_64" ;;
      aarch64) PROTOC_ARCH="aarch_64" ;;
      *) echo "ERROR: unsupported architecture for protoc: $(uname -m)"; exit 1 ;;
    esac
    command -v unzip >/dev/null 2>&1 || (apt-get update && apt-get install -y --no-install-recommends unzip)
    PROTOC_TMPDIR="$(mktemp -d)"
    bash scripts/run-with-retry.sh --retries 3 --delay 15 -- \
      curl --proto '=https' --tlsv1.3 -LSsf \
      "https://github.com/protocolbuffers/protobuf/releases/download/v${PROTOC_VERSION}/protoc-${PROTOC_VERSION}-linux-${PROTOC_ARCH}.zip" \
      -o "${PROTOC_TMPDIR}/protoc.zip"
    unzip -q "${PROTOC_TMPDIR}/protoc.zip" -d "${PROTOC_TMPDIR}/protoc"
    install -m 0755 "${PROTOC_TMPDIR}/protoc/bin/protoc" /usr/local/bin/protoc
    rm -rf "${PROTOC_TMPDIR}"
    protoc --version

# Clone `threema-protocols`. Exports `THREEMA_PROTOCOLS_DIR` for the script section.
.linux_threema_protocols_before_script: &linux_threema_protocols_before_script
  - |
    set -euo pipefail
    export THREEMA_PROTOCOLS_DIR="${CI_PROJECT_DIR}/.protocols/threema-protocols"
    rm -rf "${THREEMA_PROTOCOLS_DIR}"
    bash scripts/run-with-retry.sh --retries 3 --delay 15 -- \
      git clone "https://gitlab-ci-token:${CI_JOB_TOKEN}@${THREEMA_PROTOCOLS_REPO}" "${THREEMA_PROTOCOLS_DIR}"

# Clone `structbuf-typescript`. Exports `STRUCTBUF_TYPESCRIPT_BIN` for the script section.
.linux_structbuf_typescript_before_script: &linux_structbuf_typescript_before_script
  - |
    set -euo pipefail
    STRUCTBUF_TYPESCRIPT_DIR="${CI_PROJECT_DIR}/.protocols/structbuf-typescript"
    STRUCTBUF_TYPESCRIPT_REF="$(node -p "require('./packages/protocol/package.json').pins['structbuf-typescript']")"
    rm -rf "${STRUCTBUF_TYPESCRIPT_DIR}"
    bash scripts/run-with-retry.sh --retries 3 --delay 15 -- \
      git clone "https://gitlab-ci-token:${CI_JOB_TOKEN}@${STRUCTBUF_TYPESCRIPT_REPO}" "${STRUCTBUF_TYPESCRIPT_DIR}"
    git -C "${STRUCTBUF_TYPESCRIPT_DIR}" checkout "${STRUCTBUF_TYPESCRIPT_REF}"
    bash scripts/run-with-retry.sh --retries 3 --delay 15 -- \
      npm --prefix "${STRUCTBUF_TYPESCRIPT_DIR}" ci
    export STRUCTBUF_TYPESCRIPT_BIN="${STRUCTBUF_TYPESCRIPT_DIR}/structbuf-typescript-bin.js"

# Regenerate the third party license file and fail if anything drifts.
diff:desktop:licenses:
  stage: test
  needs: []
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Regenerate `LICENSE-3RD-PARTY.txt`.
    - pnpm run generate:desktop:licenses
    # Ensure that there are no local changes to the generated file.
    - |
      DIFF="$(git diff)"
      if [[ -n "$DIFF" ]]; then
        echo -e "\e[31mFound local changes after running 'pnpm run generate:desktop:licenses'. Third party licenses out of sync?\e[0m"
        echo -e "\n$ git diff:\n$DIFF"
        exit 1
      else
        echo -e "\e[32mNo changes detected after running 'pnpm run generate:desktop:licenses'\e[0m"
      fi

# Regenerate the desktop-internal protobuf modules and fail if anything drifts.
diff:desktop:protobuf:
  stage: test
  needs: []
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
    - *linux_protoc_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Regenerate the desktop-internal protobuf modules.
    - pnpm run generate:desktop:protobuf
    # Ensure that there are no local changes to the generated files.
    - |
      DIFF="$(git diff)"
      if [[ -n "$DIFF" ]]; then
        echo -e "\e[31mFound local changes after running 'pnpm run generate:desktop:protobuf'. Internal protobuf out of sync (or wrong protoc version)?\e[0m"
        echo -e "\n$ git diff:\n$DIFF"
        exit 1
      else
        echo -e "\e[32mNo changes detected after running 'pnpm run generate:desktop:protobuf'\e[0m"
      fi

# Regenerate the protobuf bindings in `@threema/protocol` and fail if anything drifts.
diff:protocol:protobuf:
  stage: test
  needs: []
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
    - *linux_threema_protocols_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Regenerate the protobuf bindings at the commit pinned in `@threema/protocol`.
    - pnpm run generate:protocol:protobuf -- "$THREEMA_PROTOCOLS_DIR"
    # Ensure that there are no local changes to the generated files.
    - |
      DIFF="$(git diff)"
      if [[ -n "$DIFF" ]]; then
        echo -e "\e[31mFound local changes after running 'pnpm run generate:protocol:protobuf'. Protobuf bindings out of sync?\e[0m"
        echo -e "\n$ git diff:\n$DIFF"
        exit 1
      else
        echo -e "\e[32mNo changes detected after running 'pnpm run generate:protocol:protobuf'\e[0m"
      fi

# Regenerate the structbuf bindings in `@threema/protocol` and fail if anything drifts.
diff:protocol:structbuf:
  stage: test
  needs: []
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
    - *linux_threema_protocols_before_script
    - *linux_structbuf_typescript_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Regenerate the structbuf bindings at the commit pinned in `@threema/protocol`.
    - pnpm run generate:protocol:structbuf -- "$STRUCTBUF_TYPESCRIPT_BIN" "$THREEMA_PROTOCOLS_DIR"
    # Ensure that there are no local changes to the generated files.
    - |
      DIFF="$(git diff)"
      if [[ -n "$DIFF" ]]; then
        echo -e "\e[31mFound local changes after running 'pnpm run generate:protocol:structbuf'. Structbuf bindings out of sync?\e[0m"
        echo -e "\n$ git diff:\n$DIFF"
        exit 1
      else
        echo -e "\e[32mNo changes detected after running 'pnpm run generate:protocol:structbuf'\e[0m"
      fi

# Regenerate the safe enums of every package with a schema and fail if anything drifts.
diff:safe-enums:
  stage: test
  needs: []
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Regenerate the safe enums of every package that declares a `generate:safe-enums` task.
    - pnpm run generate:safe-enums
    # Ensure that there are no local changes to the generated files.
    - |
      DIFF="$(git diff)"
      if [[ -n "$DIFF" ]]; then
        echo -e "\e[31mFound local changes after running 'pnpm run generate:safe-enums'. Generated enums out of sync?\e[0m"
        echo -e "\n$ git diff:\n$DIFF"
        exit 1
      else
        echo -e "\e[32mNo changes detected after running 'pnpm run generate:safe-enums'\e[0m"
      fi

diff:pnpm-lock:
  stage: test
  needs: []
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    # Note: No `pnpm ci` or `pnpm install --frozen-lockfile`, so `pnpm-lock.yaml` is updated if
    # there are changes.
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm install
    # Ensure that there are no local changes to `pnpm-lock.yaml`.
    - |
      DIFF="$(git diff)"
      if [[ -n "$DIFF" ]]; then
        echo -e "\e[31mFound local changes after running 'pnpm install'. Lockfile out of sync?\e[0m"
        echo -e "\n$ git diff:\n$DIFF"
        exit 1
      else
        echo -e "\e[32mNo changes detected after running 'pnpm install'\e[0m"
      fi

diff:translations:
  stage: test
  needs: []
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Parse i18n translations found in code into translation files.
    - pnpm run generate:desktop:translations
    # Ensure that there are no local changes to the translation files
    - |
      DIFF="$(git diff)"
      if [[ -n "$DIFF" ]]; then
        echo -e "\e[31mFound local changes after running 'pnpm run generate:desktop:translations'. Translations out of sync?\e[0m"
        echo -e "\n$ git diff:\n$DIFF"
        exit 1
      else
        echo -e "\e[32mNo changes detected after running 'pnpm run generate:desktop:translations'\e[0m"
      fi

validate:desktop:flatpak:metainfo:
  stage: test
  needs: []
  before_script:
    - apt-get update && apt-get install -y appstream-util
  script:
    - set -euo pipefail
    - |
      G_DEBUG=fatal-criticals
      SUCCESS_VALIDATE=1
      appstream-util validate apps/desktop/packaging/metadata/ch.threema.threema-desktop.metainfo.xml || SUCCESS_VALIDATE=0
      if [[ "$SUCCESS_VALIDATE" -ne 1 ]]; then
        # If regular validation failed, validate with "validate-relax". If that
        # succeeds, mark the build as "successful with warnings" (though the
        # custom exit code).
        echo "Validation with 'validate' failed. Retrying with 'validate-relax'."
        appstream-util validate-relax apps/desktop/packaging/metadata/ch.threema.threema-desktop.metainfo.xml
        exit 123
      fi
  allow_failure:
    exit_codes:
      - 123

validate:peer-dependencies-met:
  stage: test
  needs: []
  <<: *linux_nodejs_config
  before_script:
    - set -euo pipefail
    - *linux_nodejs_before_script
  script:
    - set -euo pipefail
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    - pnpm peers check

validate:socket:
  stage: test
  needs: []
  image:
    name: socketdev/cli:2.2.86
    entrypoint: ['']
  script:
    - |
      socketcli --version
      socketcli \
        --repo "$CI_PROJECT_NAME" \
        --branch $(if [ -z $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME ]; then echo "$CI_COMMIT_BRANCH"; else echo "$CI_MERGE_REQUEST_SOURCE_BRANCH_NAME"; fi) \
        --target-path $CI_PROJECT_DIR \
        --commit-message "$CI_COMMIT_MESSAGE" \
        --commit-sha "$CI_COMMIT_SHA" \
        --pr-number $(if [ -z $CI_MERGE_REQUEST_IID ]; then echo 0; else echo $CI_MERGE_REQUEST_IID; fi) \
        --scm gitlab \
        $(if [ "$CI_DEFAULT_BRANCH" == "$CI_COMMIT_BRANCH" ] || [ "$CI_DEFAULT_BRANCH" == "$CI_COMMIT_REF_NAME" ]; then echo --default_branch; fi)

validate:rust-versions-in-sync:
  stage: test
  needs: []
  script:
    - |
      set -euo pipefail

      _files=(
        "apps/desktop/src/rust/common/rust-toolchain.toml"
        "apps/desktop/src/rust/helper/rust-toolchain.toml"
        "apps/desktop/src/rust/launcher/rust-toolchain.toml"
        "packages/libthreema-wasm/libs/libthreema/rust-toolchain.toml"
      )

      for _file in "${_files[@]}"; do
        _version=$(sed -n -E 's/^[[:space:]]*channel[[:space:]]*=[[:space:]]*"([^"]*)".*/\1/p' "$_file")
        if [ "$_version" != "$RUST_CHANNEL" ]; then
          echo "ERROR: ${_file} uses ${_version}, expected ${RUST_CHANNEL} (from RUST_CHANNEL in .gitlab-ci.yml)"
          exit 1
        fi
      done
      if [[ "$RUST_VERSION" != "${RUST_CHANNEL}."* && "$RUST_VERSION" != "$RUST_CHANNEL" ]]; then
        echo "ERROR: RUST_VERSION (${RUST_VERSION}) does not start with RUST_CHANNEL (${RUST_CHANNEL})"
        exit 1
      fi
      echo "All toolchain files and RUST_VERSION (${RUST_VERSION}) are consistent with RUST_CHANNEL (${RUST_CHANNEL})"

# Package stage: Flatpak

.linux_flatpak_config: &linux_flatpak_config
  stage: package
  needs: [libthreema]
  # Build using a privileged runner, required for Flatpak. See:
  # https://github.com/flatpak/flatpak/issues/3027.
  tags:
    - docker-privileged
  cache:
    # Cache NodeJS-related paths. Note: Keep this in sync with `*linux_nodejs_config`.
    - key:
        prefix: linux
        files:
          - package.json
          - pnpm-lock.yaml
      paths:
        - .corepack/
        - .nvm/
        - .pnpm-store/
    # Cache Flatpak-related paths (`flatpak-builder` and `ccache`).
    - key: $CI_COMMIT_REF_SLUG
      paths:
        - apps/desktop/packaging/flatpak/.flatpak-builder/
  before_script:
    - set -euo pipefail
    # Base dependencies.
    - apt-get update
    - apt-get install -y flatpak jq lftp elfutils libyaml-dev libglib2.0-dev libcurl4-gnutls-dev libxml2-dev libxslt1-dev python3-aiohttp python3-toml
    # Set up Rust (Cargo) environment.
    - *linux_rust_before_script
    # Set up NodeJS environment.
    - *linux_nodejs_before_script
    # Set up git.
    - git config --global init.defaultBranch main # Avoid warning on stdout.
    - git config --global advice.detachedHead false # Don't show detached head warnings.
    # Build current version of `flatpak-builder`.
    - apt-get install -y meson cmake debugedit appstream appstream-compose libjson-glib-dev libelf-dev libostree-dev
    - |
      pushd /opt/
      wget https://github.com/flatpak/flatpak-builder/releases/download/1.4.3/flatpak-builder-1.4.3.tar.xz
      tar xf flatpak-builder-1.4.3.tar.xz
      cd flatpak-builder-1.4.3
      meson setup --prefix=/usr --buildtype=release -Ddocs=disabled _build
      meson install -C _build
      popd
    # Add `flathub` remote.
    - flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
    # Import PGP key.
    - KEYFILE_ENV_VAR="GPG_KEY_${GPG_ID}"; gpg --import ${!KEYFILE_ENV_VAR}
    # Run SSH agent.
    - eval $(ssh-agent -s)
    # Add SSH deploy key.
    - chmod 400 $SSH_DEPLOY_KEY && ssh-add $SSH_DEPLOY_KEY
  script:
    - set -euo pipefail
    # Handle ref.
    - |
      echo "\$CI_COMMIT_REF_NAME=$CI_COMMIT_REF_NAME"
      if [[ "$CI_COMMIT_REF_NAME" = "develop" ]]; then
        export BUILD_TYPE=nightly
      elif [[ "$CI_COMMIT_REF_NAME" = v* ]]; then
        export BUILD_TYPE=release
      elif [[ "$PUBLISH" = "false" ]]; then
        export BUILD_TYPE=nightly
      else
        echo "Error: Invalid ref"
        exit 1
      fi
      echo "Build type is $BUILD_TYPE"
    # Install build script dependencies.
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Determine some build-type-dependent parameters.
    - |
      case "$BUILD_TYPE" in
        nightly)
          BRANCH=master
          REPO="$REPO-nightly"
          REMOTE="$REMOTE-nightly"
          ;;
        release)
          BRANCH=master
          ;;
        *)
          echo "Invalid build type: $BUILD_TYPE"
          exit 1
      esac
    # Patch metadata for nightlies.
    - |
      if [ "$BUILD_TYPE" = "nightly" ]; then
        VERSION=$(jq -r '.version' package.json)
        GIT_REVISION=$(git rev-parse --short HEAD)
        DATE=$(date +%Y-%m-%d)
        # Metainfo file
        sed -i "s/\(^.*version=\"$VERSION\".*\)/<release version=\"${VERSION}-nightly-${GIT_REVISION}\" date=\"$DATE\" \/>\n\1/" apps/desktop/packaging/metadata/ch.threema.threema-desktop.metainfo.xml
      fi
    # Split flavors.
    - IFS=',' read -ra flavors <<< "$FLAVORS"
    # Build Flatpak package.
    - |
      export THREEMADESKTOP_FLATPAK_BRANCH=$BRANCH
      export THREEMADESKTOP_FLATPAK_GPG_KEY=$GPG_ID
      for flavor in "${flavors[@]}"; do
        pnpm run "package:desktop:$flavor"
      done
    # Create `.flatpakref` files.
    - |
      for flavor in "${flavors[@]}"; do
        case "$flavor" in
          consumer-sandbox)
            APPID="ch.threema.threema-green-desktop"
            TITLE="Threema Green Beta"
            ;;
          consumer-live)
            APPID="ch.threema.threema-desktop"
            TITLE="Threema Beta"
            ;;
          work-sandbox)
            APPID="ch.threema.threema-blue-desktop"
            TITLE="Threema Blue Beta"
            ;;
          work-live)
            APPID="ch.threema.threema-work-desktop"
            TITLE="Threema Work Beta"
            ;;
          work-onprem)
            APPID="ch.threema.threema-onprem-desktop"
            TITLE="Threema OnPrem Beta"
            ;;
          *)
            echo "Invalid build flavor: $flavor"
            exit 1
        esac

        FREF="apps/desktop/packaging/flatpak/repo/${APPID}.flatpakref"
        cat <<__EOF__ > $FREF
      [Flatpak Ref]
      Version=1
      Name=$APPID
      Branch=$BRANCH
      Title=$TITLE
      Homepage=https://threema.ch/
      Url=$REPO_BASE_URL/$REPO/
      SuggestRemoteName=$REMOTE
      RuntimeRepo=https://dl.flathub.org/repo/flathub.flatpakrepo
      IsRuntime=false
      GPGKey=$(gpg --export $GPG_ID | base64 --wrap=0)
      __EOF__
        echo "$FREF:"
        cat $FREF
        echo "---"
      done
    # Copy screenshot(s).
    - cp apps/desktop/packaging/flatpak/screenshot-* apps/desktop/packaging/flatpak/repo/
    # Dump gpg public key.
    - gpg --export --armor $GPG_ID > apps/desktop/packaging/flatpak/repo/signkey.pub
    # Sync to server.
    - |
      if [ "$PUBLISH" = "true" ]; then
        mkdir -p sync
        mv apps/desktop/packaging/flatpak/repo sync/$REPO
        echo "Syncing $REPO to FTP server"
        lftp -e "mirror -R sync/$REPO /$REPO; exit" -u $FTP_USER,"$FTP_PASS" $FTP_HOST
      else
        echo "Skipping publication, \$PUBLISH=$PUBLISH"
      fi

# An optional, manually-triggered, build-but-not-publish Flatpak job.
package:desktop:flatpak:preview:
  <<: *linux_flatpak_config
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
      when: never
    - if: $CI_COMMIT_TAG =~ /^v[0-9a-z\.-]*$/
      when: never
    - when: manual
  allow_failure: true # Job is optional.
  variables:
    <<: *linux_nodejs_variables
    FLAVORS: consumer-sandbox,consumer-live,work-sandbox,work-live,work-onprem
    GPG_ID: BF7BBB57363D6233178AC7CCE4D4F1B275BB5D72
    PUBLISH: 'false'
    REMOTE: threema-internal
    REPO_BASE_URL: $FLATPAK_INTERNAL_REPO_BASE_URL
    REPO: repo

package:desktop:flatpak:publish:internal:
  <<: *linux_flatpak_config
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
    - if: $CI_COMMIT_TAG =~ /^v[0-9a-z\.-]*$/
  variables:
    <<: *linux_nodejs_variables
    FLAVORS: consumer-sandbox,consumer-live,work-sandbox,work-live,work-onprem
    FTP_HOST: $FLATPAK_INTERNAL_FTP_HOST
    FTP_PASS: $FLATPAK_INTERNAL_FTP_PASS
    FTP_USER: flatpak-internal
    GPG_ID: BF7BBB57363D6233178AC7CCE4D4F1B275BB5D72
    PUBLISH: 'true'
    REMOTE: threema-internal
    REPO_BASE_URL: $FLATPAK_INTERNAL_REPO_BASE_URL
    REPO: repo

# Note: Public builds are also pushed to the internal fileserver, and are then published using a
# separate script.
package:desktop:flatpak:publish:public:
  <<: *linux_flatpak_config
  rules:
    - if: $CI_COMMIT_TAG =~ /^v[0-9a-z\.-]*$/
  variables:
    <<: *linux_nodejs_variables
    FLAVORS: consumer-live,work-live
    FTP_HOST: $FLATPAK_PUBLIC_FTP_HOST
    FTP_PASS: $FLATPAK_PUBLIC_FTP_PASS
    FTP_USER: flatpak-internal
    GPG_ID: 8BC90B39A3DEA043F88607CA74463EA2AF62F4C6
    PUBLISH: 'true'
    REMOTE: threema-desktop
    REPO_BASE_URL: $FLATPAK_PUBLIC_REPO_BASE_URL
    REPO: threema-desktop

# Package stage: macOS

# Common environment variables for macOS-based jobs that require a NodeJS environment.
.macos_nodejs_variables: &macos_nodejs_variables
  COREPACK_ENABLE_DOWNLOAD_PROMPT: '0'
  COREPACK_HOME: '$CI_PROJECT_DIR/.corepack'
  PNPM_CONFIG_STORE_DIR: '$CI_PROJECT_DIR/.pnpm-store'

.macos_config: &macos_config
  stage: package
  needs: [libthreema]
  parallel:
    matrix:
      - OS: [macos]
        ARCH: [arm64, x64]
  tags:
    - ${OS}-${ARCH}
  cache:
    key:
      prefix: macos
      files:
        - package.json
        - pnpm-lock.yaml
    paths:
      - .corepack/
      - .pnpm-store/
  artifacts:
    paths:
      - build/dmg/
    expire_in: 2d
  before_script:
    - set -euo pipefail
    # Determine build type from ref name.
    - |
      echo "\$CI_COMMIT_REF_NAME=$CI_COMMIT_REF_NAME"
      if [[ "$CI_COMMIT_REF_NAME" = v* ]]; then
        export BUILD_TYPE=release
      else
        export BUILD_TYPE=nightly
      fi
      echo "Build type is $BUILD_TYPE"

    # Print `nvm` version.
    - nvm --version

    # Install NodeJS and use version from `.nvmrc`.
    - nvm install && nvm use
    - node --version

    # Enable corepack shims, so pnpm uses the `packageManager` version defined in `package.json`.
    - corepack enable
    - pnpm --version

    # Ensure `coreutils` are installed.
    - brew install coreutils
  script:
    - set -euo pipefail
    # Install build script dependencies.
    - bash scripts/run-with-retry.sh --retries 3 --delay 15 -- pnpm ci
    # Patch version.
    - |
      if [ "$BUILD_TYPE" = "nightly" ]; then
        sed -i '' \
          's/"version": "\([^"]*\)"/"version": "\1-dev-'$(date +"%Y%m%d")'-'$(git rev-parse --short HEAD)'"/' \
          apps/desktop/package.json
      fi
    # Split flavors.
    - IFS=',' read -ra flavors <<< "$FLAVORS"
    # Build DMG packages.
    - |
      for flavor in "${flavors[@]}"; do
        pnpm run "package:desktop:$flavor"
      done
    # Rename and upload DMGs.
    - mkdir -p build/dmg/
    - |
      for flavor in "${flavors[@]}"; do
        # Determine filename.
        case "$flavor" in
          consumer-sandbox)
            FILENAME="threema-green-desktop"
            DMG="ThreemaGreen.dmg"
            ;;
          consumer-live)
            FILENAME="threema-desktop"
            DMG="Threema.dmg"
            ;;
          work-sandbox)
            FILENAME="threema-blue-desktop"
            DMG="ThreemaBlue.dmg"
            ;;
          work-live)
            FILENAME="threema-work-desktop"
            DMG="ThreemaWork.dmg"
            ;;
          work-onprem)
            FILENAME="threema-onprem-desktop"
            DMG="ThreemaOnPrem.dmg"
            ;;
          *)
            echo "Invalid build flavor: $flavor"
            exit 1
        esac
        case "$BUILD_TYPE" in
          nightly)
            FILENAME+="-nightly-$OS-$ARCH.dmg"
            ;;
          release)
            FILENAME+="-$CI_COMMIT_REF_NAME-$OS-$ARCH.dmg"
            ;;
          *)
            echo "Invalid build type: $BUILD_TYPE"
            exit 1
        esac

        # Copy files (used for artifacts).
        for ext in '' '.sha256' '.b2'; do
          echo "Processing $FILENAME$ext"
          cp "build/installers/mac/$DMG$ext" "build/dmg/$FILENAME$ext"
          if [ -n "$ext" ]; then
            cd build/dmg/
            # Patch checksum file.
            sed -i '' "s/$DMG/$FILENAME/" "$FILENAME$ext"
            # Test checksum.
            ${ext:1}sum -c $FILENAME$ext
            cd -
          fi
        done
        ls -lah build/dmg/

        # Upload
        if [ "$PUBLISH" = "true" ]; then
          case "$BUILD_TYPE" in
            nightly)
              # Upload, overwrite if it exists.
              curl -T "build/dmg/$FILENAME" "${FTP_PROTO}://${FTP_HOST}${FTP_PATH}/nightly/$FILENAME" --user "$FTP_USER:$FTP_RELEASE_PASS"
              ;;
            release)
              # Ensure that build wasn't already uploaded.
              FTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" -I "${FTP_PROTO}://${FTP_HOST}${FTP_PATH}/releases/$FILENAME" --user "$FTP_USER:$FTP_RELEASE_PASS" || true)
              if [ "$FTP_STATUS" = "350" ]; then echo "File already exists"; exit 42; fi
              # Upload DMG and checksums.
              for ext in '' '.sha256' '.b2'; do
                curl -T "build/dmg/$FILENAME$ext" "${FTP_PROTO}://${FTP_HOST}${FTP_PATH}/releases/$FILENAME$ext" --user "$FTP_USER:$FTP_RELEASE_PASS"
              done
              ;;
            *)
              echo "Invalid build type: $BUILD_TYPE"
              exit 1
          esac
        else
          echo "Skipping publication, \$PUBLISH=$PUBLISH"
        fi
      done

# An optional, manually-triggered, build-but-not-publish macOS package job without signing.
package:desktop:macos:preview:unsigned:
  <<: *macos_config
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
      when: never
    - if: $CI_COMMIT_TAG =~ /^v[0-9a-z\.-]*$/
      when: never
    - when: manual
  allow_failure: true # Job is optional.
  variables:
    <<: *macos_nodejs_variables
    FLAVORS: consumer-sandbox,consumer-live,work-sandbox,work-live,work-onprem
    PUBLISH: 'false'
    TURBO_PACKAGE_SIGNATURE: 'false'

# An optional, manually-triggered, build-but-not-publish macOS package job with signing.
package:desktop:macos:preview:signed:
  <<: *macos_config
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
      when: never
    - if: $CI_COMMIT_TAG =~ /^v[0-9a-z\.-]*$/
      when: never
    - when: manual
  allow_failure: true # Job is optional.
  variables:
    <<: *macos_nodejs_variables
    FLAVORS: consumer-sandbox,consumer-live,work-sandbox,work-live,work-onprem
    PUBLISH: 'false'
    TURBO_PACKAGE_SIGNATURE: 'true'

# Build and publish macOS packages.
package:desktop:macos:publish:
  <<: *macos_config
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
    - if: $CI_COMMIT_TAG =~ /^v[0-9a-z\.-]*$/
  variables:
    <<: *macos_nodejs_variables
    FLAVORS: consumer-sandbox,consumer-live,work-sandbox,work-live,work-onprem
    FTP_HOST: $FTP_RELEASE_HOST
    FTP_PATH: /
    FTP_PROTO: $FTP_RELEASE_PROTO
    # Set `FTP_RELEASE_PASS` masked variable in CI!
    FTP_USER: threema-desktop
    PUBLISH: 'true'
    TURBO_PACKAGE_SIGNATURE: 'true'

# Package stage: Windows

.windows_config: &windows_config
  stage: package
  needs: [libthreema]
  <<: *windows_nodejs_config
  artifacts:
    paths:
      - build\out\
    expire_in: 2d
  before_script:
    # Determine build type from ref name.
    - |
      echo "`$CI_COMMIT_REF_NAME=$env:CI_COMMIT_REF_NAME"
      if ($env:CI_COMMIT_REF_NAME -eq "develop") {
        $BUILD_TYPE = "nightly"
      } elseif ($env:CI_COMMIT_REF_NAME -like "v*") {
        $BUILD_TYPE = "release"
      } elseif ($env:PUBLISH -eq "false") {
        $BUILD_TYPE = "nightly"
      } else {
        throw "Error: Invalid ref"
      }
      echo "Build type is $BUILD_TYPE"
    - *windows_nodejs_before_script
    - *windows_rust_before_script
  script:
    # Install dependencies.
    - pnpm ci

    # Build MSIX packages.
    - |
      foreach ($flavor in "consumer-live","work-sandbox","work-live","work-onprem") {
        $parts = $flavor -split '-'
        $env:TURBO_BUILD_VARIANT = $parts[0]
        $env:TURBO_BUILD_ENVIRONMENT = $parts[1]
        pnpm run package:desktop
      }

    # Upload.
    - dir build\out\
    - |
      if ($env:PUBLISH -eq "true") {
        foreach ($flavor in "consumer-live","work-sandbox","work-live","work-onprem") {
          switch ($flavor) {
            "consumer-live" {
              $name = "threema"
            }
            "work-sandbox" {
              $name = "threema-blue"
            }
            "work-live" {
              $name = "threema-work"
            }
            "work-onprem" {
              $name = "threema-onprem"
            }
          }
          $sourceFileExe = "build/out/${name}-desktop-bin-win32-${env:ARCH}.zip"
          $sourceFileMsix = "build/out/${name}-desktop-windows-${env:ARCH}.msix"
          switch ($BUILD_TYPE) {
            "nightly" {
              $filenameExe = "$name-desktop-nightly-windows-${env:ARCH}.zip"
              $filenameMsix = "$name-desktop-nightly-windows-${env:ARCH}.msix"

              # Upload, overwrite if it exists.
              echo "Uploading $filenameExe"
              curl.exe -T "$sourceFileExe" "${env:FTP_PROTO}://${env:FTP_HOST}${env:FTP_PATH}/nightly/$filenameExe" --user "${env:FTP_USER}:${env:FTP_RELEASE_PASS}"
              echo "Uploading $filenameMsix"
              curl.exe -T "$sourceFileMsix" "${env:FTP_PROTO}://${env:FTP_HOST}${env:FTP_PATH}/nightly/$filenameMsix" --user "${env:FTP_USER}:${env:FTP_RELEASE_PASS}"
            }
            "release" {
              $filenameExe = "$name-desktop-$CI_COMMIT_REF_NAME-windows-${env:ARCH}.zip"
              $filenameMsix = "$name-desktop-$CI_COMMIT_REF_NAME-windows-${env:ARCH}.msix"

              # Ensure that build wasn't already uploaded.
              $ftp_status = (curl.exe -o NUL -s -w "%{http_code}" -I "${env:FTP_PROTO}://${env:FTP_HOST}${env:FTP_PATH}/releases/$filenameExe" --user "${env:FTP_USER}:${env:FTP_RELEASE_PASS}")
              If ($ftp_status -eq 350) {
                throw "File $filenameExe already exists on FTP server"
              }
              $ftp_status = (curl.exe -o NUL -s -w "%{http_code}" -I "${env:FTP_PROTO}://${env:FTP_HOST}${env:FTP_PATH}/releases/$filenameMsix" --user "${env:FTP_USER}:${env:FTP_RELEASE_PASS}")
              If ($ftp_status -eq 350) {
                throw "File $filenameMsix already exists on FTP server"
              }

              # Upload ZIP, MSIX and checksums.
              Foreach ($ext in '','.sha256') {
                echo "Uploading $filenameExe$ext"
                curl.exe -T "$sourceFileExe$ext" "${env:FTP_PROTO}://${env:FTP_HOST}${env:FTP_PATH}/releases/$filenameExe$ext" --user "${env:FTP_USER}:${env:FTP_RELEASE_PASS}"
              }
              Foreach ($ext in '','.sha256') {
                echo "Uploading $filenameMsix$ext"
                curl.exe -T "$sourceFileMsix$ext" "${env:FTP_PROTO}://${env:FTP_HOST}${env:FTP_PATH}/releases/$filenameMsix$ext" --user "${env:FTP_USER}:${env:FTP_RELEASE_PASS}"
              }
            }
            default {
              throw "Error: Invalid build type: $BUILD_TYPE"
            }
          }
        }
      } else {
        echo "Skipping publication, \$PUBLISH=$env:PUBLISH"
      }

# An optional, manually-triggered, build-but-not-publish Windows package job without signing.
package:desktop:windows:preview:unsigned:
  <<: *windows_config
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
      when: never
    - if: $CI_COMMIT_TAG =~ /^v[0-9a-z\.-]*$/
      when: never
    - when: manual
  allow_failure: true # Job is optional.
  variables:
    ARCH: 'x64'
    PUBLISH: 'false'
    TURBO_PACKAGE_SIGNATURE: 'false'

# An optional, manually-triggered, build-but-not-publish Windows package job with signing.
package:desktop:windows:preview:signed:
  <<: *windows_config
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
      when: never
    - if: $CI_COMMIT_TAG =~ /^v[0-9a-z\.-]*$/
      when: never
    - when: manual
  allow_failure: true # Job is optional.
  variables:
    ARCH: 'x64'
    PUBLISH: 'false'
    TURBO_PACKAGE_SIGNATURE: 'true'

# Build and publish Windows packages.
package:desktop:windows:publish:
  <<: *windows_config
  rules:
    - if: $CI_COMMIT_BRANCH == 'develop'
    - if: $CI_COMMIT_TAG =~ /^v[0-9a-z\.-]*$/
  variables:
    ARCH: 'x64'
    FTP_HOST: $FTP_RELEASE_HOST
    FTP_PATH: /
    FTP_PROTO: $FTP_RELEASE_PROTO
    # Set FTP_RELEASE_PASS masked variable in CI!
    FTP_USER: threema-desktop
    PUBLISH: 'true'
    TURBO_PACKAGE_SIGNATURE: 'true'
